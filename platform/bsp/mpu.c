#include "mpu.h"
int mpu_configure_region(unsigned idx, void *base, unsigned size, unsigned attr){ (void)idx; (void)base; (void)size; (void)attr; return 0; }
void mpu_enable(int enable){ (void)enable; }
