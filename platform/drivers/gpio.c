#include "gpio.h"
int gpio_mode(int pin, gpio_mode_t mode){ (void)pin; (void)mode; return 0; }
int gpio_write(int pin, int value){ (void)pin; (void)value; return 0; }
int gpio_read(int pin){ (void)pin; return 0; }
